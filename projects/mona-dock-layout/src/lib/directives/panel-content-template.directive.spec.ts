import { PanelContentTemplateDirective } from './panel-content-template.directive';

describe('PanelContentTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelContentTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
