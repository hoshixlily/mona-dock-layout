import { PanelContentAnchorDirective } from './panel-content-anchor.directive';

describe('PanelContentAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelContentAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
