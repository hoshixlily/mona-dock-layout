import { LayoutContentTemplateDirective } from './layout-content-template.directive';

describe('LayoutContentTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new LayoutContentTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
