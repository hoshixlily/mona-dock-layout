import { PanelTitleTemplateDirective } from './panel-title-template.directive';

describe('PanelTitleTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelTitleTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
