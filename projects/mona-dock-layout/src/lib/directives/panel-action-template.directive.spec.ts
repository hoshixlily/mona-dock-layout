import { PanelActionTemplateDirective } from './panel-action-template.directive';

describe('PanelActionTemplateDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelActionTemplateDirective();
    expect(directive).toBeTruthy();
  });
});
