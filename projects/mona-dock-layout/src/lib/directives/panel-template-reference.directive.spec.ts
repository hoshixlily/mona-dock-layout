import { PanelTemplateReferenceDirective } from './panel-template-reference.directive';

describe('PanelTemplateReferenceDirective', () => {
  it('should create an instance', () => {
    const directive = new PanelTemplateReferenceDirective();
    expect(directive).toBeTruthy();
  });
});
