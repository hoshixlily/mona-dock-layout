import { PanelPositionSelectorPipe } from './panel-position-selector.pipe';

describe('PanelPositionSelectorPipe', () => {
  it('create an instance', () => {
    const pipe = new PanelPositionSelectorPipe();
    expect(pipe).toBeTruthy();
  });
});
